# Руководство по синхронизации платежей с Google Sheets

## Обзор

Реализована полная система синхронизации платежей между БД SQLite и Google Sheets с автоматическим созданием отдельных листов для каждой роли пользователей.

---

## 🎯 Основные возможности

### ✅ Реализовано

1. **Отдельные листы для каждой роли** в Google Sheets:
   - `Վճարումներ - Ադմինիստրատոր` (Платежи - Администратор)
   - `Վճարումներ - Աշխատող` (Платежи - Работник)
   - `Վճարումներ - Երկրորդային` (Платежи - Вторичный)
   - `Վճարումներ - Կլիենտ` (Платежи - Клиент)

2. **Автоматическое создание листов** при инициализации бота

3. **Двойная запись платежей**: БД + Google Sheets

4. **Синхронизация при старте бота**: загрузка платежей из Sheets в БД

5. **Автоматическая синхронизация** при добавлении нового платежа

---

## 📋 Структура таблицы платежей

### Колонки в Google Sheets

| # | Колонка | Армянский | Описание |
|---|---------|-----------|----------|
| 1 | ID | ID | Уникальный ID платежа из БД |
| 2 | User Display Name | Անուն | Имя получателя платежа |
| 3 | Amount | Գումար | Сумма платежа |
| 4 | Date From | Սկզբնական ամսաթիվ | Начало периода (опционально) |
| 5 | Date To | Վերջնական ամսաթիվ | Конец периода (опционально) |
| 6 | Comment | Մեկնաբանություն | Комментарий к платежу |
| 7 | Created At | Ստեղծման ամսաթիվ | Дата и время создания |
| 8 | Spreadsheet ID | Աղյուսակի ID | ID таблицы для двойной записи |
| 9 | Sheet Name | Թերթիկի անուն | Имя листа для двойной записи |

**Примечание:** Колонки 8-9 используются для функционала двойной записи (когда платеж работнику также записывается как расход в другую таблицу).

---

## 🚀 Настройка

### 1. Создайте Google Sheets таблицу для платежей

1. Создайте новую таблицу в Google Sheets
2. Скопируйте ID таблицы из URL:
   ```
   https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit
   ```

### 2. Предоставьте доступ Service Account

1. Откройте вашу таблицу
2. Нажмите "Share" (Поделиться)
3. Добавьте email вашего Service Account:
   ```
   your-service-account@project-id.iam.gserviceaccount.com
   ```
4. Предоставьте права "Editor" (Редактор)

### 3. Настройте переменные окружения

Добавьте в файл `.env`:

```env
# ID таблицы для платежей
PAYMENTS_SPREADSHEET_ID=1abc123def456ghi789jkl
```

### 4. Запустите бота

При запуске бота автоматически:
- Создадутся 4 листа (по одному для каждой роли)
- Добавятся заголовки колонок
- Применится форматирование (жирный шрифт, фон)
- Загрузятся платежи из Sheets в БД (если есть)

---

## 📁 Архитектура

### Созданные модули

#### 1. [src/google_integration/payments_sheets_manager.py](src/google_integration/payments_sheets_manager.py)

**Класс:** `PaymentsSheetsManager`

**Основные методы:**
- `initialize_payment_sheets()` - создает листы для всех ролей
- `add_payment_to_sheet()` - добавляет платеж в соответствующий лист
- `get_payments_from_sheet(role)` - получает платежи из листа роли
- `get_all_payments_from_sheets()` - получает все платежи
- `delete_payment_from_sheet()` - удаляет платеж

**Константы:**
- `SHEET_NAMES` - названия листов для каждой роли
- `HEADERS` - заголовки колонок

#### 2. [src/google_integration/payments_sync_manager.py](src/google_integration/payments_sync_manager.py)

**Класс:** `PaymentsSyncManager`

**Основные методы:**
- `sync_payments_from_sheets_to_db()` - синхронизация Sheets → БД
- `sync_payment_to_sheets()` - синхронизация одного платежа БД → Sheets
- `full_sync_payments()` - полная синхронизация
- `get_sync_status()` - статус синхронизации
- `validate_sync()` - проверка синхронизированности

---

## 🔄 Workflow синхронизации

### При старте бота

```
1. Инициализация таблицы платежей
   ↓
2. Проверка существования листов для каждой роли
   ↓
3. Создание недостающих листов
   ↓
4. Добавление/проверка заголовков
   ↓
5. Загрузка всех платежей из Google Sheets
   ↓
6. Поиск платежей, которых нет в БД
   ↓
7. Добавление недостающих платежей в БД
   ↓
8. Логирование статистики
```

### При добавлении нового платежа

```
1. Администратор добавляет платеж через бота
   ↓
2. Платеж сохраняется в БД
   ↓
3. Получение ID нового платежа
   ↓
4. Определение роли получателя платежа
   ↓
5. Синхронизация платежа в соответствующий лист Google Sheets
   ↓
6. Логирование успеха/ошибки
```

---

## 💡 Примеры использования

### Пример 1: Добавление платежа программно

```python
from src.database.database_manager import add_payment

# Добавляем платеж (автоматически синхронизируется с Sheets)
payment_id = add_payment(
    user_display_name="Արամ",
    amount=50000,
    date_from="2025-10-01",
    date_to="2025-10-31",
    comment="Зарплата за октябрь"
)

if payment_id > 0:
    print(f"✅ Платеж #{payment_id} добавлен и синхронизирован")
```

### Пример 2: Ручная синхронизация

```python
from src.google_integration.payments_sync_manager import PaymentsSyncManager

sync_manager = PaymentsSyncManager()

# Полная синхронизация
stats = sync_manager.full_sync_payments()
print(f"Добавлено: {stats['added']}, Пропущено: {stats['skipped']}")

# Проверка статуса
status = sync_manager.get_sync_status()
print(f"БД: {status['db_count']}, Sheets: {status['sheets_count']}")

# Проверка синхронизированности
if sync_manager.validate_sync():
    print("✅ Все синхронизировано")
else:
    print("⚠️ Есть несинхронизированные платежи")
```

### Пример 3: Получение платежей по роли

```python
from src.google_integration.payments_sheets_manager import PaymentsSheetsManager
from src.config.settings import UserRole

manager = PaymentsSheetsManager()

# Получить платежи работников
worker_payments = manager.get_payments_from_sheet(UserRole.WORKER)
print(f"Платежей работникам: {len(worker_payments)}")

# Получить все платежи
all_payments = manager.get_all_payments_from_sheets()
print(f"Всего платежей: {len(all_payments)}")
```

---

## 🔍 Мониторинг и отладка

### Логи

Все операции логируются с префиксами:
- `✅` - успешная операция
- `⚠️` - предупреждение
- `❌` - ошибка
- `🔄` - процесс синхронизации
- `📊` - статистика

### Проверка синхронизации

Вызовите в коде:

```python
from src.google_integration.payments_sync_manager import PaymentsSyncManager

sync_manager = PaymentsSyncManager()
status = sync_manager.get_sync_status()

print(f"""
📊 Статус синхронизации:
- В БД: {status['db_count']} платежей
- В Sheets: {status['sheets_count']} платежей
- Синхронизировано: {status['synced']}
- Не в Sheets: {status['in_db_not_in_sheets']}
- Не в БД: {status['in_sheets_not_in_db']}
""")
```

---

## ⚠️ Важные замечания

### 1. ID платежей

- ID платежа в Google Sheets **должен** совпадать с ID в БД
- ID генерируется автоматически при добавлении в БД
- НЕ изменяйте ID вручную в Google Sheets

### 2. Ручное редактирование в Sheets

При ручном добавлении платежа в Google Sheets:
- ✅ Платеж будет загружен в БД при следующем запуске бота
- ⚠️ Укажите уникальный ID (или оставьте пустым)
- ⚠️ Соблюдайте формат данных

### 3. Роли и листы

- Каждая роль имеет свой отдельный лист
- При добавлении платежа роль определяется автоматически
- Платежи разных ролей НЕ смешиваются

### 4. Производительность

- Синхронизация выполняется асинхронно
- При большом количестве платежей (>1000) синхронизация может занять до минуты
- Рекомендуется не вызывать синхронизацию слишком часто

---

## 🐛 Известные проблемы и решения

### Проблема 1: "PAYMENTS_SPREADSHEET_ID не установлен"

**Решение:**
- Добавьте `PAYMENTS_SPREADSHEET_ID` в `.env`
- Перезапустите бота

### Проблема 2: "Не удалось открыть таблицу платежей"

**Решение:**
- Проверьте, что Service Account имеет доступ к таблице
- Проверьте правильность SPREADSHEET_ID

### Проблема 3: Платежи дублируются

**Решение:**
- Проверьте, что ID платежей уникальны
- Используйте `validate_sync()` для проверки
- Удалите дубликаты вручную из Sheets или БД

---

## 📊 Статистика использования

После запуска бота проверьте логи:

```
📊 Инициализация таблицы платежей...
✅ Создан лист: Վճարումներ - Աշխատող
✅ Создан лист: Վճարումներ - Կլիենտ
... (остальные листы)
✅ Таблица платежей инициализирована

🔄 Синхронизация платежей...
В БД найдено 15 платежей
В Google Sheets найдено 12 платежей
✅ Добавлен платеж #16 из Google Sheets в БД (получатель: Արամ)
✅ Добавлен платеж #17 из Google Sheets в БД (получатель: Տիկո)
✅ Синхронизация завершена. Добавлено: 2, Пропущено: 10, Ошибок: 0

✅ Синхронизация платежей завершена. Добавлено: 2, Пропущено: 10, Ошибок: 0
```

---

## 🎓 Расширенные возможности (TODO)

Планируется добавить:

- [ ] Команда `/sync_payments` для ручной синхронизации
- [ ] Команда `/payments_status` для проверки статуса
- [ ] Автоматическая синхронизация по расписанию (каждый час)
- [ ] Уведомления о несинхронизированных платежах
- [ ] Экспорт отчета о платежах в Excel
- [ ] История изменений платежей

---

## 📚 Связанная документация

- [ROLES_IMPLEMENTATION.md](ROLES_IMPLEMENTATION.md) - Система ролей
- [TODO.md](TODO.md) - Оставшиеся задачи

---

Последнее обновление: 2025-10-22
