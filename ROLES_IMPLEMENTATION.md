# Реализация системы ролей и управления пользователями

## Статус реализации

### ✅ РЕАЛИЗОВАНО

#### 1. Система ролей
- **Константы ролей** добавлены в [src/config/settings.py](src/config/settings.py):
  - `UserRole.SUPER_ADMIN` - Супер-администратор (управление ролями)
  - `UserRole.ADMIN` - Администратор (управление платежами)
  - `UserRole.WORKER` - Работник (добавление записей)
  - `UserRole.SECONDARY` - Вторичный (только просмотр платежей)
  - `UserRole.CLIENT` - Клиент (только получение уведомлений)

- **Супер-администратор** через переменную окружения `SUPER_ADMIN_ID` в [.env.example](.env.example)

#### 2. Утилиты управления ролями
Добавлены в [src/utils/config_utils.py](src/utils/config_utils.py):

**Функции получения роли:**
- `get_user_role(user_id)` - Получает роль пользователя
- `set_user_role(user_id, role)` - Устанавливает роль
- `get_users_by_role(role)` - Список пользователей по роли

**Проверки ролей:**
- `is_super_admin(user_id)` - Супер-админ
- `is_admin(user_id)` - Админ или супер-админ
- `is_worker(user_id)` - Работник
- `is_secondary(user_id)` - Вторичный
- `is_client(user_id)` - Клиент

**Проверки прав:**
- `can_add_records(user_id)` - Может добавлять записи
- `can_edit_records(user_id)` - Может редактировать
- `can_view_payments(user_id)` - Может просматривать платежи
- `can_add_payments(user_id)` - Может добавлять платежи
- `can_manage_users(user_id)` - Может управлять пользователями

**Вспомогательные:**
- `get_role_display_name(role)` - Отображаемое имя роли на армянском

#### 3. Обработчики управления ролями
Созданы в [src/bot/handlers/role_management_handlers.py](src/bot/handlers/role_management_handlers.py):

**Главное меню:**
- `/roles` - Команда открытия меню управления ролями
- Кнопка "👥 Управление ролями" в главном меню для супер-админа

**Функции меню:**
- ➕ Добавить пользователя (с выбором роли)
- 👥 Список всех пользователей
- ✏️ Изменить роль пользователя
- 🗑 Удалить пользователя
- 👁 Вторичные пользователи (список read-only пользователей)

**ConversationHandler для добавления пользователя:**
1. Ввод User ID
2. Ввод отображаемого имени (на армянском)
3. Выбор роли из меню
4. Сохранение в `users.json` и `allowed_users.json`

#### 4. Обновленное главное меню
Обновлено в [src/bot/keyboards/inline_keyboards.py](src/bot/keyboards/inline_keyboards.py):

**Меню по ролям:**
- **Super Admin**: Все функции + "👥 Управление ролями"
- **Admin**: Платежи, добавление записей
- **Worker**: Добавление записей, просмотр своих платежей
- **Secondary**: Только "👁 Просмотр платежей"
- **Client**: "⛔ У вас нет доступа к меню"

#### 5. Регистрация обработчиков
Обновлено в [src/main.py](src/main.py):
- Зарегистрирован ConversationHandler для добавления пользователей
- Зарегистрированы callback-обработчики для управления ролями
- Добавлена команда `/roles`

---

## 🔄 ОСТАЛОСЬ РЕАЛИЗОВАТЬ

### 1. Синхронизация платежей с Google Sheets

**Задача:** Платежи должны сохраняться не только в БД, но и в Google Sheets

**Файлы для изменения:**
- [src/database/database_manager.py](src/database/database_manager.py)
- [src/google_integration/sync_manager.py](src/google_integration/sync_manager.py)
- [src/bot/handlers/payment_handlers.py](src/bot/handlers/payment_handlers.py)

**Что нужно сделать:**
- [ ] Создать отдельную Google Sheets таблицу для платежей
- [ ] Добавить функцию `sync_payments_to_sheet()` в sync_manager.py
- [ ] Обновить `add_payment()` в database_manager.py для двойной записи
- [ ] Добавить функцию загрузки платежей из Google Sheets при старте бота
- [ ] Реализовать проверку и синхронизацию при несоответствии данных

**Структура таблицы платежей в Google Sheets:**
```
ID | User Display Name | Amount | Date From | Date To | Comment | Created At | Spreadsheet ID | Sheet Name
```

---

### 2. Выбор типа получателя при добавлении платежа

**Задача:** При добавлении платежа администратор должен выбрать тип получателя (Работник/Клиент)

**Файлы для изменения:**
- [src/bot/handlers/payment_handlers.py](src/bot/handlers/payment_handlers.py)
- [src/bot/states/conversation_states.py](src/bot/states/conversation_states.py)

**Что нужно сделать:**
- [ ] Добавить state `SELECT_RECIPIENT_TYPE` в conversation_states.py
- [ ] Добавить меню выбора типа получателя после выбора пользователя
- [ ] Добавить выбор таблицы Google Sheets для записи
- [ ] Сохранять тип получателя в БД

**Структура workflow:**
```
Выбор пользователя → Выбор типа (Работник/Клиент) → Выбор таблицы → Ввод суммы → Период → Комментарий → Сохранение
```

---

### 3. Двойная запись платежей (расход + таблица)

**Задача:** При платеже Работнику записывать в `records` (расход) и в выбранную таблицу

**Файлы для изменения:**
- [src/bot/handlers/payment_handlers.py](src/bot/handlers/payment_handlers.py)
- [src/database/database_manager.py](src/database/database_manager.py)

**Что нужно сделать:**
- [ ] Добавить параметр `recipient_type` в функцию add_payment
- [ ] При типе "Worker" создавать запись в `records` как расход
- [ ] Генерировать уникальный ID в формате `cb-xxxxxxxx`
- [ ] Записывать в указанную Google Sheets таблицу
- [ ] Обеспечить атомарность операций

**Пример записи расхода:**
```python
{
    'id': 'cb-payment-xxxxxxxx',
    'date': payment_date,
    'supplier': user_display_name,
    'direction': 'Зарплата',
    'description': f'Выплата {date_from} - {date_to}',
    'amount': -payment_amount,  # Отрицательная сумма
    'spreadsheet_id': selected_spreadsheet_id,
    'sheet_name': selected_sheet_name,
    'user_id': admin_user_id,
    'created_at': timestamp
}
```

---

### 4. Функционал "Получено" для клиентов

**Задача:** Добавить кнопку "Получено" для записи входящих платежей от клиентов

**Файлы для изменения:**
- [src/bot/handlers/payment_handlers.py](src/bot/handlers/payment_handlers.py)
- [src/bot/keyboards/inline_keyboards.py](src/bot/keyboards/inline_keyboards.py)

**Что нужно сделать:**
- [ ] Добавить кнопку "💰 Получено" в меню администратора
- [ ] Создать список клиентов (пользователи с ролью CLIENT)
- [ ] Создать conversation для ввода суммы платежа
- [ ] Отправлять уведомление клиенту через Telegram
- [ ] Записывать в БД и Google Sheets

**Workflow:**
```
Кнопка "Получено" → Список клиентов → Ввод суммы → Подтверждение → Уведомление клиенту
```

**Текст уведомления клиенту:**
```
✅ Получен платеж

Сумма: {amount} ֏
Дата: {date}

Спасибо за оплату!
```

---

### 5. Ограничения доступа для ролей

**Задача:** Обеспечить проверку прав доступа во всех обработчиках

**Файлы для изменения:**
- [src/bot/handlers/record_handlers.py](src/bot/handlers/record_handlers.py)
- [src/bot/handlers/edit_handlers.py](src/bot/handlers/edit_handlers.py)
- [src/bot/handlers/payment_handlers.py](src/bot/handlers/payment_handlers.py)
- [src/bot/handlers/search_commands.py](src/bot/handlers/search_commands.py)

**Что нужно сделать:**
- [ ] Добавить проверку `can_add_records()` в обработчики добавления записей
- [ ] Добавить проверку `can_edit_records()` в обработчики редактирования
- [ ] Добавить проверку `can_view_payments()` в обработчики просмотра платежей
- [ ] Добавить проверку `can_add_payments()` в обработчики добавления платежей
- [ ] Добавить проверку `is_client()` для блокировки доступа к меню

**Пример проверки:**
```python
async def add_record_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    user_id = update.effective_user.id

    if not can_add_records(user_id):
        await update.message.reply_text("⛔ У вас нет прав для добавления записей.")
        return ConversationHandler.END

    # ... остальной код
```

---

### 6. Обработчик для вторичных пользователей

**Задача:** Реализовать просмотр платежей для вторичных пользователей

**Файлы для создания/изменения:**
- [src/bot/handlers/secondary_handlers.py](src/bot/handlers/secondary_handlers.py) (новый файл)

**Что нужно сделать:**
- [ ] Создать обработчик `view_payments_secondary()`
- [ ] Показывать список всех пользователей
- [ ] Показывать платежи выбранного пользователя (read-only)
- [ ] Добавить фильтры (по периоду, по пользователю)
- [ ] Зарегистрировать обработчик в main.py

---

## 📋 ЧЕКЛИСТ ДЛЯ ПРОВЕРКИ

### Проверка системы ролей

1. **Супер-администратор**
   - [ ] Может открыть меню управления ролями через кнопку
   - [ ] Может добавить нового пользователя
   - [ ] Может изменить роль существующего пользователя
   - [ ] Может удалить пользователя
   - [ ] Может просмотреть список всех пользователей
   - [ ] Может просмотреть список вторичных пользователей

2. **Администратор**
   - [ ] Видит кнопку "💸 Платежи"
   - [ ] Может добавлять платежи
   - [ ] Может добавлять записи расходов
   - [ ] НЕ видит кнопку управления ролями

3. **Работник**
   - [ ] Видит кнопки добавления записей
   - [ ] Видит кнопку "💰 Мои платежи"
   - [ ] НЕ видит админские функции

4. **Вторичный**
   - [ ] Видит кнопку "👁 Просмотр платежей"
   - [ ] НЕ может добавлять/редактировать записи
   - [ ] Может просматривать платежи всех пользователей

5. **Клиент**
   - [ ] НЕ имеет доступа к меню бота
   - [ ] Получает уведомления о платежах
   - [ ] НЕ может использовать команды бота

### Проверка синхронизации

- [ ] Платеж записывается в БД `payments`
- [ ] Платеж записывается в Google Sheets (таблица платежей)
- [ ] При старте бота загружаются платежи из Google Sheets
- [ ] При несоответствии данных выполняется синхронизация

### Проверка двойной записи

- [ ] Платеж работнику создает запись расхода в `records`
- [ ] Запись расхода имеет отрицательную сумму
- [ ] Запись расхода записывается в выбранную Google Sheets таблицу
- [ ] ID записи уникален и имеет формат `cb-payment-xxxxxxxx`

### Проверка функционала "Получено"

- [ ] Кнопка "Получено" доступна администраторам
- [ ] Показывается список клиентов
- [ ] После ввода суммы отправляется уведомление клиенту
- [ ] Платеж записывается в БД и Google Sheets

### Проверка ограничений доступа

- [ ] Клиент не может использовать /menu
- [ ] Вторичный не может добавлять записи
- [ ] Работник не может управлять платежами
- [ ] Только супер-админ может управлять ролями

---

## 🔧 НАСТРОЙКА

### 1. Настройка супер-администратора

Добавьте в файл `.env`:
```env
SUPER_ADMIN_ID=123456789
```

Где `123456789` - ваш Telegram User ID.

### 2. Первоначальная настройка ролей

После запуска бота супер-администратор может:
1. Открыть меню: `/roles`
2. Добавить пользователей с нужными ролями
3. Настроить доступ

### 3. Миграция существующих пользователей

Существующие пользователи автоматически получают роли:
- Пользователи из `ADMIN_IDS` → `ADMIN`
- Пользователи из `allowed_users.json` → `WORKER`
- Супер-админ → `SUPER_ADMIN`

---

## 📝 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Добавление нового работника
```
1. Супер-админ: /roles
2. Нажать "➕ Добавить пользователя"
3. Ввести User ID: 987654321
4. Ввести имя: Արամ
5. Выбрать роль: "Աշխատող"
6. ✅ Пользователь добавлен
```

### Изменение роли пользователя
```
1. Супер-админ: /roles
2. Нажать "✏️ Изменить роль"
3. Выбрать пользователя из списка
4. Выбрать новую роль
5. ✅ Роль изменена
```

### Просмотр вторичных пользователей
```
1. Администратор: открыть главное меню
2. Нажать "👁 Вторичные пользователи"
3. Просмотреть список
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Безопасность**: Супер-администратор имеет полный контроль над системой. Устанавливайте SUPER_ADMIN_ID только для доверенного пользователя.

2. **Резервное копирование**: Перед изменением ролей рекомендуется создать резервную копию `users.json` и `allowed_users.json`.

3. **Логирование**: Все действия по управлению ролями логируются в системный лог.

4. **Миграция данных**: При первом запуске с новой системой ролей существующие пользователи автоматически получат соответствующие роли.

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ

На данный момент известных проблем нет.

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

- Описание ролей: См. [src/config/settings.py](src/config/settings.py) → `UserRole`
- Утилиты: См. [src/utils/config_utils.py](src/utils/config_utils.py)
- Обработчики: См. [src/bot/handlers/role_management_handlers.py](src/bot/handlers/role_management_handlers.py)
